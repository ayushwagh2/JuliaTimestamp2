using SearchLight

# SearchLight.Migration.create_migrations_table()

using Emails

Email()
L=Email(name="john",lastname="wagh")
 
m=save!(L) 
save(q)
m.lastname="soemthign"
save!()

n=payload(:name)
p=find(Email, name=n)
if length(p)!=0
 
    find(Email,lastname="haish")

save!

SearchLight.Migrations.up("CreateTableEmails")
 

SearchLight.Migration.add_column(:emails, :counter, :integer)

SearchLight.Migrations.status()


